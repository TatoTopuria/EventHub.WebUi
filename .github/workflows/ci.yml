name: ci

on:
  push:
    branches: [main]
    paths-ignore: ['**.md']
  pull_request:
    paths-ignore: ['**.md']
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_VERSION: '8.0.x'
  DOTNET_NOLOGO: '1'
  DOTNET_CLI_TELEMETRY_OPTOUT: '1'

jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: read
    env:
      EVENTHUB_NUGET_FEED: ${{ vars.EVENTHUB_NUGET_FEED || 'https://nuget.pkg.github.com/tatotopuria/index.json' }}
      EVENTHUB_NUGET_USER: ${{ github.actor }}
      EVENTHUB_NUGET_TOKEN: ${{ secrets.EVENTHUB_NUGET_TOKEN || secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          source-url: ${{ vars.EVENTHUB_NUGET_FEED || 'https://nuget.pkg.github.com/tatotopuria/index.json' }}
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.EVENTHUB_NUGET_TOKEN || secrets.GITHUB_TOKEN }}
      - name: Cache NuGet
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/Directory.Packages.props') }}
          restore-keys: ${{ runner.os }}-nuget-
      - name: Build + test
        run: dotnet test WebUi.slnx --configuration Release --logger "console;verbosity=normal"

  image:
    needs: build-test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Downcase owner
        id: meta
        run: echo "owner=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build + push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: src/EventHub.WebUi/Dockerfile
          push: true
          tags: ghcr.io/${{ steps.meta.outputs.owner }}/eventhub-webui:latest
          build-args: |
            EVENTHUB_NUGET_FEED=${{ vars.EVENTHUB_NUGET_FEED || 'https://nuget.pkg.github.com/tatotopuria/index.json' }}
            EVENTHUB_NUGET_USER=${{ github.actor }}
            EVENTHUB_NUGET_TOKEN=${{ secrets.EVENTHUB_NUGET_TOKEN || secrets.GITHUB_TOKEN }}